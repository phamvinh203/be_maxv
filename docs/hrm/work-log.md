# Work Log — HRM

## [2026-09-09 10:51] backend-engineer — code mới
- Nhiệm vụ: Nối API thật cho Cài đặt lương (Danh mục khoản lương, Cấu trúc lương khung, Set lương nhân viên) vào FE `hdđt_maxv`, thay `mock/hooks/khoanLuong.ts` + `mock/hooks/setLuong.ts` (giữ nguyên file mock theo yêu cầu).
- Đã sửa:
  - `be_maxv/prisma/tenant/schema.prisma`:1189 (enum `CalculationMethod` thêm `REVENUE_PERCENTAGE`, `KPI_BASED`, `MANUAL_ENTRY` — khớp đủ 7 tiêu thức tính của FE thay vì chỉ 4)
  - `be_maxv/src/validators/hrm/cai_dat_luong/salaryStructures.validator.ts`:5 (`CALCULATION_METHODS` +3 giá trị)
  - `be_maxv/src/services/client/hrm/cai_dat_luong/salaryStructures.service.ts`:104 (kiểu `resolvedItems` suy từ hằng validator thay vì liệt kê literal union cứng, tránh lệch khi enum đổi)
  - `hdđt_maxv/src/features/hrm/api/salaryItemsApi.ts` (mới), `salaryItemsQueries.ts` (mới) — Danh mục khoản lương
  - `hdđt_maxv/src/features/hrm/api/salaryStructuresApi.ts` (mới), `salaryStructuresQueries.ts` (mới) — Cấu trúc lương khung
  - `hdđt_maxv/src/features/hrm/api/employeeSalariesApi.ts` (mới), `employeeSalariesQueries.ts` (mới) — Set lương nhân viên
  - `hdđt_maxv/src/features/hrm/api/hrmKeys.ts`:57 (thêm `hrmSalaryItemKeys`, `hrmSalaryStructureKeys`, `hrmEmployeeSalaryKeys`)
  - `hdđt_maxv/src/features/hrm/components/cai_dat_luong/CaiDatLuongPanel.tsx`:7, `TaoDanhMucPanel.tsx`:9, `KhoanLuongTable.tsx`:21, `KhoanLuongFormDialog.tsx`:18 (đổi import từ mock sang API thật)
  - `hdđt_maxv/src/features/hrm/components/cai_dat_luong/set_luong/SetLuongPanel.tsx`:14-15, `CauTrucLuongCard.tsx`:24, `DanhSachSetLuongCard.tsx`:31-35 (đổi import từ mock sang API thật)
  - `hdđt_maxv/src/features/hrm/components/cai_dat_luong/set_luong/SetLuongNhanVienDialog.tsx`:22-25 (đổi import từ mock sang API thật; đồng thời sửa `useNhanVienDetail` đang trỏ `mock/hooks/nhanVien` — không map được nhân viên thật nên dialog không mở được — sang bản thật `api/nhanVienQueries.ts`, phát hiện khi test tay)
- Liên kết: docs/hrm/cai_dat_luong/srs-cai-dat-luong.md · docs/hrm/cai_dat_luong/api-contract-cai-dat-luong.md (lưu ý: doc này lệch so với route/response thật ở vài chỗ — xem ghi chú trong code, chưa cập nhật lại doc)
- Kiểm chứng: `tsc --noEmit` be_maxv sạch · `tsc -b` hdđt_maxv sạch · `eslint` các file mới/sửa sạch · 23/23 test be_maxv (`hrmSalarySettings.test.ts` + `hrmSalarySettingsApi.test.ts`) pass · test tay trên browser thật (tài khoản vinhh@1trick.net, công ty CÔNG TY TNHH ĐẦU TƯ SẢN XUẤT VÀ XNK THÀNH CÔNG): tạo đủ 7 khoản lương (1 khoản/loại), sửa 1 khoản, xóa khoản đang dùng bị chặn đúng (E-sal-003), lưu + đọc lại cấu trúc lương đủ 7 tiêu thức tính (tổng 15.700.000đ khớp), set lương + duyệt lương nhân viên NV0001 (tổng 17.700.000đ khớp) — giữ lại làm dữ liệu demo theo yêu cầu.
- Commit: chưa commit

## [2026-09-09 11:37] backend-engineer — /simplify (dọn code, không sửa nghiệp vụ)
- Nhiệm vụ: Chạy `/simplify` (4 agent song song: reuse/simplification/efficiency/altitude) trên toàn bộ diff `be_maxv` của phiên làm việc trên, áp các fix an toàn (không đổi hành vi).
- Đã sửa:
  - `be_maxv/src/helpers/resolveTenantDb.ts`:22 — **bỏ backdoor test** `if ((req as any).mockDb) return ...` khỏi helper dùng chung cho hơn chục controller (phát hiện độc lập bởi 2/4 agent — mức nghiêm trọng nhất). Test chuyển sang `node:test` `mock.module()` (đã verify thực nghiệm: namespace-mutation KHÔNG hoạt động dưới ESM thật của tsx, phải patch qua loader hook trước khi route import).
  - `be_maxv/src/__tests__/hrmSalarySettingsApi.test.ts` — viết lại `buildTestApp()` dùng `before()` + `mock.module('../helpers/resolveTenantDb')` + `import()` động 3 route module (route/controller thật, chỉ mock đúng 1 hàm).
  - `be_maxv/package.json`:26 — thêm cờ `--experimental-test-module-mocks` vào script `test` (bắt buộc để `mock.module` hoạt động).
  - `salaryStructures.service.ts` — export `toIsoDate` dùng chung (xóa bản trùng ở `employeeSalaries.service.ts`); xóa biến chết `activeSet`; đổi vòng lặp `create` → `createMany` khi lưu dòng cấu trúc lương.
  - `employeeSalaries.service.ts` — `Promise.all` hóa 3 chỗ query độc lập chạy nối tiếp (`listEmployeeSalaries`, `getEmployeeSalary`, `setEmployeeSalary`); bỏ `findMany` thừa trong `approveEmployeeSalaries` (dùng thẳng `updateMany` + `result.count`); đổi vòng lặp `create` → `createMany`; bỏ nhánh xử lý `khoan` chết trong `setEmployeeSalary` (dead code từ khi `SetEmployeeSalaryInput` đổi sang `z.infer`); đơn giản Set→Array thừa trong `countEmployeeSalaries`; type `where: any` → `Prisma.EmployeeSalaryWhereInput`.
  - `salaryItems.service.ts` — gộp check trùng tên (create/update) vào `assertSalaryItemNameAvailable()`; `Promise.all` hóa 2 count() trong `deleteSalaryItem`.
  - `salaryItems.validator.ts` — hoist `description`/`defaultRate` thành field dùng chung create+update.
  - `employeeSalaries.validator.ts` — gộp schema `hasSalary`/`daSet` trùng lặp thành `optionalBooleanFlag`; dùng `??` thay ternary lồng; đổi `SetEmployeeSalaryInput` từ `z.input` sang `z.infer` (khớp với việc bỏ dead code ở service).
  - `hrmSalarySettings.test.ts` — cập nhật theo các thay đổi trên: mock `tx` thêm `createMany`, thêm `salaryStructure.findFirst` stub cho ca test BR-sal-008 (giờ tải song song), đổi 1 lời gọi trực tiếp `setEmployeeSalary` từ payload `{khoan}` thô sang `{items}`.
- Đã CHỦ Ý bỏ qua (ghi rõ lý do, không phải quên):
  - Gộp `generateNextSalaryItemCode` với `sinhMaCa()` (workShifts) thành 1 helper dùng chung — đụng file ngoài diff phiên này.
  - Đổi "current active contract" sang dùng `hopDongHienHanhTheoNv` có sẵn — khác tie-break logic, đổi hành vi biên, thuộc phạm vi /code-review chứ không phải /simplify.
  - Đổi validator ngày `effectiveFrom`/`effectiveTo` sang `ngayISO`/`ngayTuyChon` dùng chung — sẽ CHẶN các ngày lịch không tồn tại hiện đang lọt qua (đổi hành vi validation), để dành /code-review.
  - Áp dụng `helpers/crudGuards.ts` (`findOrThrow`/`assertNotExists`) — refactor cơ học nhưng diện rộng (~10 chỗ/3 file), để dành phiên riêng.
  - Tightening kiểu `any` của `formatEmployeeSalary`/`formatSalaryStructure` — rủi ro/công sức không tương xứng lợi ích thuần type-safety.
- Liên kết: session /simplify trên diff của mục "code mới" phía trên cùng file.
- Kiểm chứng: `tsc --noEmit` sạch · `eslint` 0 lỗi (74 warning, toàn bộ đã có từ trước — `any` trong test file theo quy ước sẵn có, không warning mới) · `npm test` 631/635 pass — 4 fail còn lại đều ở `hrmSettingsShiftsHolidaysApi.test.ts` (TC-hrm-301 tìm kiếm bỏ dấu, TC-hrm-316 thiếu trường "thứ trong tuần"), xác nhận qua `git status` là file/feature hoàn toàn không đụng tới trong cả 2 phiên (không phải regression).
- Commit: chưa commit

## [2026-09-09 11:55] backend-engineer — /simplify (hdđt_maxv, dọn code, không sửa nghiệp vụ)
- Nhiệm vụ: Chạy `/simplify` (4 agent song song) trên diff frontend (`hdđt_maxv`) của tính năng Cài đặt lương — 3 file API/Queries mới + `hrmKeys.ts` + 8 component đổi import.
- Đã sửa:
  - `hdđt_maxv/src/features/hrm/api/employeeSalariesQueries.ts` — **thêm debounce 300ms cho ô tìm kiếm** trong `useSetLuongRows` (phát hiện bởi agent Efficiency: mỗi ký tự gõ đang bắn 1 request riêng, so với bản mock cũ lọc client-side không tốn mạng — verify bằng browser thật: gõ liền "Vinh" 4 ký tự cách nhau ~50ms chỉ tạo ĐÚNG 1 request `?q=Vinh`, khớp cùng ngưỡng 300ms của `CompanyFormDialog`); đổi query key sang gọi qua `hrmEmployeeSalaryKeys.list(companyId, params)` thay vì spread thủ công tại nơi gọi; sửa `mutationFn` point-free; sửa lại comment giải thích `REJECTED → cho_duyet` cho chính xác hơn (không có endpoint nào ghi trạng thái này, không chỉ vì thiếu nút UI).
  - `hdđt_maxv/src/features/hrm/api/hrmKeys.ts` — mở rộng `hrmEmployeeSalaryKeys.list` nhận thêm `params` làm tham số thứ 2 (builder tự gói key), khớp quy ước `hrmHopDongKeys.list`/`hrmHolidayKeys.list` đã có (2/4 agent độc lập cùng phát hiện điểm này).
  - `hdđt_maxv/src/features/hrm/api/salaryItemsQueries.ts` — suy `CATEGORY_TO_LOAI` từ `LOAI_TO_CATEGORY` bằng `Object.fromEntries` đảo chiều thay vì viết tay 2 bảng dữ liệu giống hệt nhau; 2 `mutationFn` đổi sang dạng point-free (`mutationFn: createSalaryItem` thay vì bọc closure thừa).
  - `hdđt_maxv/src/features/hrm/api/salaryStructuresQueries.ts` — suy `CALC_METHOD_TO_TIEU_THUC` từ chiều ngược lại tương tự; thêm `useLamMoi()` cho khớp quy ước 4 file còn lại trong cùng tính năng (trước đó tự inline invalidate khác kiểu).
- Đã CHỦ Ý bỏ qua: đổi tên quy ước `MAP_X`/`MAP_X_NGUOC` (đã có ở `holidaysQueries.ts`/`cauHinhQueries.ts`, ngoài diff) sang `X_TO_Y` cho đồng nhất toàn bộ — chỉ là nit đặt tên, không sửa vì đụng file ngoài phạm vi phiên này.
- Liên kết: session /simplify tiếp theo cho phần backend cùng feature (mục ngay phía trên).
- Kiểm chứng: `tsc -b --force` sạch · `eslint` 0 lỗi 0 warning trên 4 file sửa · test tay trên browser thật: debounce xác nhận đúng 1 request/lần gõ dừng, dữ liệu cấu trúc lương + set lương hiển thị lại đúng y nguyên sau khi sửa (không mất dữ liệu, không đổi hành vi).
## [2026-09-09 14:26] ba-engineer — Phân tích đặc tả & Đối chiếu mock component Dữ liệu tính lương
- Nhiệm vụ: Nghiên cứu sâu toàn bộ tài liệu đặc tả tại `docs/nestjs/du_lieu_tinh_luong`, đối chiếu toàn bộ 8 mock components + hooks tại `hdđt_maxv/src/features/hrm/components/du_lieu_tinh_luong`, phân tích kiến trúc Fastify + Prisma tenant của `be_maxv`.
- Đã thực hiện:
  - Lập báo cáo chi tiết `docs/hrm/du_lieu_tinh_luong/BA_ANALYSIS_SPEC.md` xác định chính xác khoảng trống:
    + 6 Enums & 14 Models mới cần bổ sung vào `be_maxv/prisma/tenant/schema.prisma` (áp dụng đúng quy chuẩn `ma_nv`, tiền tố `hrm_`).
    + Bộ 26 mã lỗi chuẩn hóa `E-dltl-001` .. `E-dltl-026`.
    + 5 Bất biến tài chính cốt lõi (Khóa sổ, chặn sàn chuyên cần, công nợ âm, snapshot đơn giá/tỷ lệ, rollback import Excel).
    + Danh mục toàn bộ RESTful API endpoints cần xây dựng tại `be_maxv/src/routes/hrm/du_lieu_tinh_luong`.
- Liên kết: docs/hrm/du_lieu_tinh_luong/BA_ANALYSIS_SPEC.md · docs/nestjs/du_lieu_tinh_luong/
- Trạng thái: Đã lưu vết và sẵn sàng lập Implementation Plan triển khai vào `be_maxv`.

## [2026-09-09 14:48] fullstack-engineer — Triển khai Tích hợp Frontend & Backend Hoàn chỉnh cho Dữ liệu tính lương
- Nhiệm vụ:
  1. Xây dựng toàn bộ hệ thống API Client & TanStack React-Query hooks tại `hdđt_maxv/src/features/hrm/api/` nối trực tiếp với backend `be_maxv`.
  2. Xây dựng bộ điều khiển Quản lý kỳ lương `KyLuongSelector` và `PayrollPeriodContext` cho khu Dữ liệu tính lương.
  3. Chuyển đổi 4 màn hình quản lý danh mục chuyên biệt (KPI, Sản phẩm, Chuyên cần, Bù trừ) và các Dialog/Cards sang API thật.
- Đã thực hiện:
  - `hdđt_maxv/src/features/hrm/api/hrmKeys.ts`: Bổ sung 4 nhóm query keys (`hrmPayrollPeriodKeys`, `hrmPayrollCatalogKeys`, `hrmPayrollDataKeys`, `hrmPayrollCalculationKeys`).
  - `hdđt_maxv/src/features/hrm/api/payrollPeriodsApi.ts` & `payrollPeriodsQueries.ts`: CRUD và toàn bộ vòng đời 6 trạng thái kỳ lương (`submit`, `reject`, `lock`, `reopen`, `approve`, `mark-paid`, `archive`).
  - `hdđt_maxv/src/features/hrm/api/payrollCatalogsApi.ts` & `payrollCatalogsQueries.ts`: CRUD 4 danh mục chuyên biệt (KPI Items, Piecework Products, Diligence Types, Adjustment Items) với adapter chuyển đổi định dạng chuẩn FE/BE.
  - `hdđt_maxv/src/features/hrm/api/payrollInputsApi.ts` & `payrollInputsQueries.ts`: 8 phân hệ nhập liệu theo kỳ (chấm công cell override, tăng ca, kpi, thưởng, lương sp, lương %, chuyên cần, bù trừ).
  - `hdđt_maxv/src/features/hrm/api/payrollCalculationApi.ts` & `payrollCalculationQueries.ts`: Tính toán preview bảng lương 18 cột và đọc snapshot sheet lines khi khóa sổ.
  - `hdđt_maxv/src/features/hrm/api/salaryItemsQueries.ts`: Bổ sung `useKhoanThuongList` và `useKhoanPhanTramList` đọc từ danh mục khoản lương thật.
  - `hdđt_maxv/src/features/hrm/components/du_lieu_tinh_luong/PayrollPeriodContext.tsx` & `useCurrentPayrollPeriod.ts`: Context lưu trữ và chia sẻ kỳ lương hiện hành xuyên suốt 8 tabs của Dữ liệu tính lương, bảo vệ trạng thái chỉ đọc khi đã khóa sổ.
  - `hdđt_maxv/src/features/hrm/components/du_lieu_tinh_luong/KyLuongSelector.tsx`: Thanh điều khiển kỳ lương trên UI với các nút chuyển trạng thái (Tạo kỳ mới, Trình duyệt, Khóa sổ, Duyệt, Mở lại kỳ lương với bắt buộc lý do >= 20 ký tự).
  - Cập nhật các Dialog & Cards (`QuanLyKpiDialog`, `BangChiTieuKpiCard`, `TaiSuDungKpiDialog`, `QuanLySanPhamDialog`, `BangSanPhamCard`, `TaiSuDungSanPhamDialog`, `QuanLyChuyenCanDialog`, `BangChuyenCanCard`, `QuanLyBuTruDialog`, `BangBuTruCard`, `TaiSuDungBuTruDialog`, `QuanLyThuongDialog`, `BangKhoanThuongCard`, `QuanLyPhanTramDialog`, `BangPhanTramCard`, `DuLieuLuongPage`) chuyển sang sử dụng API và Query Hooks thật.
- Kiểm chứng & Chất lượng:
  - Backend: `npx tsx --experimental-test-module-mocks --test src/__tests__/hrmPayrollInputData.test.ts` — **3/3 test suites pass 100%** (Lifecycle, Lock Guard, Thuế 7 bậc, Chặn sàn chuyên cần, Catalogs CRUD).
  - Frontend: `npm run lint` — **0 error, 0 warning** (exit code 0).
  - Frontend: `npm run build` (`tsc -b && vite build`) — **Build thành công** (exit code 0, 12351 modules transformed, 5.31s).

## [2026-09-09 16:40] business-analyst — Kiểm định độc lập & Hình thức hóa SRS cho Dữ liệu tính lương
- Nhiệm vụ: Kiểm định độc lập (không tin suông) toàn bộ nội dung `BA_ANALYSIS_SPEC.md` và `walkthrough.md` do phiên "ba-engineer"/"fullstack-engineer" (14:26, 14:48 cùng ngày, không qua Gate) tạo ra, đối chiếu từng Business Rule/mã lỗi/entity với mã nguồn thật đã viết trong `be_maxv` và `hdđt_maxv`, sau đó hình thức hóa thành SRS chính thức.
- Đã đọc để đối chiếu (không sửa): `git diff be_maxv/prisma/tenant/schema.prisma` · 4 service + 4 controller + 4 route + 3 validator dưới `du_lieu_tinh_luong/` · `payrollErrors.ts`, `payrollPeriodLockGuard.ts`, `errorHandler.plugin.ts` · `hrmPayrollInputData.test.ts` · 8 file `*Panel.tsx` + `ThanhLocKyLuong.tsx` + `KyLuongSelector.tsx` + `PayrollPeriodContext.tsx` ở `hdđt_maxv` · `docs/hrm/cai_dat_luong/srs-cai-dat-luong.md` (đối chiếu category `SalaryItem`) · `schema.prisma` model `GeneralSetting` (đối chiếu tham số cấu hình bị bỏ qua) · `docs/nestjs/du_lieu_tinh_luong/srs/du_lieu_tinh_luong-spec.md` (chỉ để soi độ đầy đủ cấu trúc, không lấy business rule).
- Đã tạo: `docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md` — SRS chính thức thay thế `BA_ANALYSIS_SPEC.md`, gồm 23 Business Rules (`BR-dltl-001`…`023`, mỗi rule ghi trạng thái triển khai thật kèm `file:line`), Error Matrix đầy đủ 26 mã (`E-dltl-001`…`026`, phân loại cơ chế trả về PayrollError/Zod-only/chưa triển khai), 11 Acceptance Criteria (`AC-dltl-01`…`11`), 10 câu hỏi mở (`OQ-dltl-001`…`010`), bảng chuyển trạng thái kỳ lương, 5 edge case bổ sung, và mục tổng hợp 12 phát hiện đối soát (4× 🔴, 5× 🟠, 3× 🟡 — xem chi tiết trong file).
- Đã sửa (thêm nội dung, không đổi cấu trúc cũ): `docs/hrm/CONTEXT_SUMMARY.md` — Mục 1 (thêm bullet trạng thái phân hệ mới), Mục 2 (thêm 14 dòng entity), Mục 3 (thêm 4 nhóm API map + ghi chú lệch), Mục 4 (thêm mục tài liệu #13), Mục 19 mới (toàn bộ biên bản kiểm định).
- Phát hiện quan trọng nhất (🔴, chưa từng được `BA_ANALYSIS_SPEC.md`/`walkthrough.md` nhắc tới): (1) cả 8/8 file `*Panel.tsx` — màn hình chính của 8 phân hệ nhập liệu — vẫn dùng `mock/hooks/*`, chưa nối API thật, khiến tuyên bố "100% nối frontend" của walkthrough sai cho đúng phần lõi nghiệp vụ; (2) động cơ tính lương (`payrollCalculation.service.ts`) chỉ đọc 2/~15 tham số từ `GeneralSetting` đã cấu hình sẵn, còn lại hardcode (ngày công chuẩn, giờ chuẩn/ngày, tỷ lệ bảo hiểm, đoàn phí, biểu thuế TNCN); (3) Reopen kỳ lương thiếu role-guard ADMIN-only và audit log dù đặc tả khẳng định có; (4) tạo kỳ lương không tự sinh lịch công chuẩn như đặc tả mô tả.
- Liên kết: docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md · docs/hrm/du_lieu_tinh_luong/BA_ANALYSIS_SPEC.md (giữ làm vết lịch sử) · docs/hrm/du_lieu_tinh_luong/walkthrough.md (đã đối chiếu, không tin suông) · docs/hrm/CONTEXT_SUMMARY.md Mục 19
- Kiểm chứng: Đây là đợt đọc mã tĩnh + đối soát tài liệu, KHÔNG tự chạy lại `npm test`/`npm run typecheck`/`npm run lint`/`npm run build` và KHÔNG gọi thử API runtime — mọi con số "634 tests pass"/"Build thành công" của phiên trước CHƯA được đợt này xác minh lại, chỉ đánh giá hợp lý qua đọc tĩnh. Không sửa file `.ts`/`.tsx`/`.prisma` nào (đúng ràng buộc nhiệm vụ).
- Commit: chưa commit

## [2026-09-09 17:10] architect — Kiểm định kiến trúc độc lập (Phase A, song song không đọc chéo tester-qa)
- Nhiệm vụ: Đọc `srs-du-lieu-tinh-luong.md` làm nguồn nghiệp vụ, đối chiếu ngược với `schema.prisma` diff (14 model) + toàn bộ route/controller/service/validator thật của `du_lieu_tinh_luong`, thẩm định lại 4 phát hiện 🔴 của BA và tự săn thêm lỗi kiến trúc (transaction boundary, N+1, kiểu dữ liệu tài chính, multi-tenant isolation).
- Đã tạo: `docs/hrm/du_lieu_tinh_luong/data-model-du-lieu-tinh-luong.md` (14 model, index/constraint, ranh giới transaction, 5 ADR: `dltl-01`..`dltl-05`) · `docs/hrm/du_lieu_tinh_luong/api-contract-du-lieu-tinh-luong.md` (45 endpoint, ma trận 26 mã lỗi kèm điểm `throw` thật).
- Thẩm định 4 phát hiện BA: #1 ✅ đúng nhưng phạm vi rộng hơn (23 file còn mock, không phải 8 — kể cả 7/7 dialog "Tái sử dụng" bị BA đánh giá nhầm là đã nối thật) · #2 ✅ đúng, chỉnh số liệu (8/23 tham số có đọc, không phải 2/15 — 6 `otRate*` đọc đúng) · #3 ✅ đúng, nặng hơn: `reason` ≥20 ký tự bị validate rồi **vứt luôn**, không lưu đâu cả · #4 ⚠️ hạ 🔴→🟠: mô hình delta là lựa chọn kiến trúc đúng (không nên sinh 26×N dòng mặc định), lỗi thật là bỏ qua `Holiday`/`saturdayPolicy` ngầm.
- Phát hiện mới (chưa báo cáo nào trước đó nhắc tới): 🔴 A-01 4 controller thiếu `assertXemLuong` — `OWNER_EMPLOYEE` có `xemLuong=false` vẫn đọc được lương/TNCN toàn công ty qua `GET /payroll/calculate` (lặp lại đúng lớp lỗi BUG-HRM-25/28 đã từng xảy ra ở phân hệ Hợp đồng) · 🔴 A-02 `payrollCalculation.service.ts:44-47` lấy hợp đồng bằng `orderBy ngay_bat_dau desc take 1` không lọc theo kỳ lương — hợp đồng ký trước cho tương lai hoặc đã hết hạn vẫn được dùng tính lương kỳ hiện tại · 🔴 A-03 `attendanceType` không bao giờ được đọc trong engine, nghỉ không lương vẫn tính đủ công · 🟠 A-04..A-08 (TOCTOU mọi state-transition, N+1 ghi trong transaction không set timeout, `/payroll/sheet-lines` đổi kiểu number→string sau khóa sổ, thiếu `Math.round` gây preview≠snapshot, thiếu validate FK/category khi tạo bản ghi 8 phân hệ).
- Phát hiện ngoài lề: `docs/hrm/cai_dat_luong/data-model-cai-dat-luong.md` (status `approved`) đã lệch schema thật ở 5 mục — không dùng làm chuẩn đối chiếu, cần một phiên riêng cập nhật lại (ngoài phạm vi feature này).
- Liên kết: docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md (BR-dltl-001..023) · docs/hrm/du_lieu_tinh_luong/data-model-du-lieu-tinh-luong.md · docs/hrm/du_lieu_tinh_luong/api-contract-du-lieu-tinh-luong.md
- Kiểm chứng: Đọc mã tĩnh, KHÔNG sửa code, KHÔNG chạy test, KHÔNG đọc kết quả tester-qa (chạy song song độc lập).
- Commit: chưa commit

## [2026-09-09 17:10] tester-qa — Thiết kế test độc lập từ SRS (Phase A, song song không đọc chéo architect)
- Nhiệm vụ: Tự thiết kế bộ kịch bản test từ `srs-du-lieu-tinh-luong.md` TRƯỚC khi đọc `hrmPayrollInputData.test.ts`, sau đó đối chiếu ngược để tìm khoảng trống coverage, tự chạy lại test thật (không tin số liệu "3/3 pass"/"634 tests pass" của walkthrough cũ), và tự săn bug logic qua đọc code 4 service.
- Đã tạo: `docs/hrm/du_lieu_tinh_luong/qa-report-du-lieu-tinh-luong.md` (test matrix 76 kịch bản, Gherkin chi tiết cho ca P0, error-code cross-check 26 mã, danh mục `BUG-dltl-001`..`013` + `ISSUE-dltl-001`..`004`).
- Kết quả tự chạy test (xác nhận độc lập, có số liệu thật): `hrmPayrollInputData.test.ts` = 3/3 PASS (888ms) · toàn bộ suite be_maxv = 634/638 PASS (4 fail thuộc `hrmSettingsShiftsHolidaysApi.test.ts`, không liên quan feature này, không phải regression) · `tsc --noEmit` 0 lỗi · `eslint` 0 error/324 warning (có sẵn từ trước) · `hdđt_maxv` build PASS.
- Phát hiện quan trọng nhất: chỉ ~16% (12-13/76) kịch bản tự thiết kế có test thật che phủ; **1 test giả** tại `hrmPayrollInputData.test.ts:417-425` ("Bất biến chặn sàn chuyên cần") — không hề gọi `payrollCalculation.service.ts`/`payrollInputs.service.ts`, chỉ tự tính lại công thức bằng biến cục bộ rồi assert với chính nó, nên nếu ai xóa `Math.max(0,...)` thật trong production code thì test này vẫn PASS; 0% test cho `GET /payroll/calculate` (349 dòng engine) và cho 6/8 phân hệ nhập liệu qua HTTP `apply`.
- Bug mới tự phát hiện: 🔴 `payrollInputs.service.ts:112-113` — `workDayValue` không chặn trần 1.0/ngày (`actualHours=24` → `workDayValue=3.0`, đường tràn dữ liệu công cụ thể) · 🟠 xác nhận độc lập cùng lỗi A-02 của architect (hợp đồng không lọc theo kỳ) · 🟠 `errorHandler.plugin.ts:113-118` trả 409 thay vì 400 cho 5 mã lỗi "ID hợp lệ format nhưng không tồn tại" · 🟡 cả BE và FE trích sai "(BR-dltl-001)" thay vì BR-dltl-002 cho rule Reopen.
- Verdict: **FAILED — chưa Ready for Production**.
- Liên kết: docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md · docs/hrm/cai_dat_luong/qa-report-cai-dat-luong.md (mẫu khung) · docs/hrm/du_lieu_tinh_luong/qa-report-du-lieu-tinh-luong.md Mục 12 (log đề xuất chi tiết)
- Kiểm chứng: Tự chạy test thật (số liệu trên), đọc mã tĩnh cho phần săn bug logic. KHÔNG sửa code/test, KHÔNG đọc kết quả architect (chạy song song độc lập).
- Commit: chưa commit

## [2026-09-09 17:40] code-reviewer — Quality Gate: review-findings.md đầu tiên của phân hệ HRM
- Nhiệm vụ: Soát code quality/security/maintainability trên toàn bộ diff `du_lieu_tinh_luong` (góc nhìn bổ sung, không lặp lại phát hiện của BA/Architect/QA — đã đọc cả 3 báo cáo trước để tránh trùng), tạo `docs/hrm/review-findings.md` (file review chính thức ĐẦU TIÊN của cả phân hệ HRM, đúng format CLAUDE.md).
- Đã tạo: `docs/hrm/review-findings.md` — Verdict ❌ Request changes. 17 finding mới (`RVW-001`..`017`: 2 🔴 Blocking, 9 🟡 Non-blocking, 6 🟢 Suggestion) + bảng 1-dòng/mục tham chiếu 15 finding đã có từ 3 báo cáo trước (không diễn giải lại).
- 2 Blocking mới, nghiêm trọng nhất toàn bộ đợt kiểm định: **RVW-001** `payrollCalculation.service.ts:152-158` — mô hình chấm công delta bị hiện thực SAI: engine coi `AttendanceRecord` (chỉ ghi qua `PUT /attendance/cell` khi có ngoại lệ) là lịch công CẢ THÁNG (`if (empAtt.length>0) actualWorkDays = sum(...)`) — hệ quả: chấm 1 NGÀY nghỉ không lương duy nhất trong tháng → `actualWorkDays = 0` (không phải ~25) → lương cơ bản tính ra = **0đ** thay vì gần đủ, và `lock` đóng băng số sai này vĩnh viễn vào `PayrollSheetLine`. Cả 3 báo cáo trước (kể cả Architect khi khen mô hình delta "lựa chọn đúng") đều không soi nhánh `if` này. **RVW-002** `payrollPeriods.controller.ts:62,70,77` — dùng `(req.user as any)?.sub`, nhưng JWT payload dự án là `{userId, donViId, role, tokenVersion}` (không có claim `sub`) → `lockedByUserId`/`approvedByUserId` luôn ghi `null`, mất dấu vết ai khóa sổ/phê duyệt; `as any` là thứ vô hiệu hóa lỗi biên dịch đáng lẽ bắt được ngay.
- Đạt (xác nhận tích cực, không phải chỉ có lỗi): không log dữ liệu lương/CCCD ra console, không raw SQL, mọi route mới thừa hưởng đúng `authenticate`+`requireModule('hrm')`, toàn bộ cột tiền dùng `Decimal(18,2)` (không lẫn `Float`).
- Liên kết: docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md · data-model-du-lieu-tinh-luong.md · api-contract-du-lieu-tinh-luong.md · qa-report-du-lieu-tinh-luong.md · docs/hrm/review-findings.md
- Kiểm chứng: Đọc mã tĩnh + tự chạy `eslint` (0 error, 16 warning mới trên diện review). KHÔNG sửa code (đúng quyền hạn code-reviewer — quyền ghi duy nhất là `review-findings.md`).
- Commit: chưa commit

## [2026-09-09 18:30] backend-engineer — fix 8/9 lỗi 🔴 sau 4 vòng kiểm định độc lập (RVW-001/002 + A-01/02/03 + BUG-dltl-002/003/004/006/007)
- Nhiệm vụ: Sửa đúng 8/9 lỗi 🔴 Blocking do BA ∥ Architect ∥ Tester-QA ∥ Code Reviewer phát hiện độc lập (`CONTEXT_SUMMARY.md` Mục 19.9, `review-findings.md`) — theo yêu cầu tường minh của user (lỗi thứ 9, 22 file FE `hdđt_maxv` còn mock, để dành `frontend-engineer`, KHÔNG đụng `hdđt_maxv`). Kèm sửa 1 test giả (BUG-dltl-006) và viết thêm test cho cả 8 lỗi.
- Đã sửa (`be_maxv/`):
  - `src/helpers/payrollAccessGuard.ts` (MỚI) — `dbCoQuyenLuongPayroll(req)` dùng chung cho A-01, gọi `resolveTenantCtx`+`assertXemLuong` (tái dùng nguyên khuôn `hopDong.controller.ts:34-38`).
  - `src/controllers/client/hrm/du_lieu_tinh_luong/payrollCalculation.controller.ts`:2,13,19 — thay `resolveTenantDb` → `dbCoQuyenLuongPayroll` (A-01).
  - `src/controllers/client/hrm/du_lieu_tinh_luong/catalogs.controller.ts` (16 điểm gọi) — thay `resolveTenantDb` → `dbCoQuyenLuongPayroll` (A-01).
  - `src/controllers/client/hrm/du_lieu_tinh_luong/payrollInputs.controller.ts` (16 điểm gọi) — thay `resolveTenantDb` → `dbCoQuyenLuongPayroll` (A-01).
  - `src/controllers/client/hrm/du_lieu_tinh_luong/payrollPeriods.controller.ts`:2-45,90-118 — thay `resolveTenantDb` → `dbCoQuyenLuongPayroll` (A-01); thêm `ghiNhatKyLuong()` gọi `writeLog` cho `lock`/`reopen`/`approve` (BUG-dltl-004); `lock`/`reopen`/`approve` đổi `(req.user as any)?.sub` → `currentUserId(req)` (RVW-002).
  - `src/routes/hrm/du_lieu_tinh_luong/payrollPeriods.route.ts`:3,14-16 — import `assertAdminOrOwner` từ `cau_hinh_mac_dinh/generalSettings.route.ts`, gắn `{ preHandler: assertAdminOrOwner }` cho `lock`/`reopen`/`approve` (BUG-dltl-003+011, mở rộng cả 3 action theo xác nhận của Architect ở `data-model-*.md` Mục 6.1).
  - `src/services/client/hrm/du_lieu_tinh_luong/payrollPeriods.service.ts`:1-11,156-201,203-218 — `lockPayrollPeriod`/`approvePayrollPeriod` đổi `userId?: string` → `userId: string` bắt buộc (RVW-002); `reopenPayrollPeriod` bỏ tham số `_input`/`_userId` không dùng (audit chuyển sang controller).
  - `src/services/client/hrm/du_lieu_tinh_luong/payrollInputs.service.ts`:108-172,153-195 — `overrideAttendanceCell`: `workDayValue` suy từ `attendanceType` qua bảng `ATTENDANCE_FIXED_VALUE` thay vì luôn dùng `actualHours` (A-03); đọc `standardHoursPerDay` từ `GeneralSetting` (BUG-dltl-002); chặn 400 `E-dltl-005` khi `actualHours > standardHoursPerDay` thay vì tràn (BUG-dltl-007). `getOvertimeData`: `isWarningMonth` đọc `GeneralSetting.maxOtHoursPerMonth` thay vì hardcode 40 (BUG-dltl-002).
  - `src/services/client/hrm/du_lieu_tinh_luong/payrollCalculation.service.ts`:1,30-77,82-121,197-249,328-334 — thêm `resolveStandardWorkDays()` (đọc `GeneralSetting.standardWorkingDaysMethod`/`Holiday`, thay hằng 26 — BUG-dltl-002); `include.hop_dong` thêm `where` lọc theo `period.startDate/endDate` (A-02); công thức delta đúng `actualWorkDays = standardWorkDays + Σ(workDayValue-1)` thay vì coi tập bản ghi là lịch cả tháng (RVW-001); tỷ lệ BHXH/BHYT/BHTN + đoàn phí đọc từ `GeneralSetting` thay vì hardcode 0.105/0.215/1%/234_000/2% (BUG-dltl-002). **Biểu thuế TNCN (`tinhThueLuyTien`) GIỮ NGUYÊN, không đụng** — đúng chỉ định tường minh của task.
  - `src/__tests__/hrmPayrollInputData.test.ts` — viết lại toàn bộ: mở rộng mock `../helpers/resolveTenantDb` thêm `resolveTenantCtx`/`assertXemLuong`/`currentUserId`; mock mới `../services/shared/syslog.service` (`writeLog`) để không đụng `sysPrisma` thật trong test; `buildTestApp()` thêm `onRequest` hook giả lập `req.user`; mock DB mở rộng: `attendanceRecord.upsert` tra `where` thật, `diligenceRecord.findMany` mô phỏng `include.violationType`, `hrm_nhan_vien.findMany` mô phỏng `include.hop_dong.where` (A-02). Sửa test giả BR-dltl-016 (BUG-dltl-006) gọi thật `getDiligenceData()`. Thêm 8 test mới, mỗi bug ≥1 test: `resolveStandardWorkDays` thuần, RVW-001 (delta 1 bản ghi), A-03 (attendanceType ghi đè actualHours sai), BUG-dltl-007 (chặn 400 E-dltl-005), A-02 (hợp đồng theo kỳ), A-01 (403 thiếu xemLuong), BUG-dltl-002 (tỷ lệ BHXH/đoàn phí từ cấu hình), Bug#8 (role-guard + audit log reopen/lock).
- Đã sửa (tài liệu, không phải code):
  - `docs/hrm/review-findings.md` — RVW-001, RVW-002: thêm dòng `→ FIXED [2026-09-09]` kèm bằng chứng. Bảng "Blocking kế thừa": thêm cột "Trạng thái (2026-09-09)" cho `A-01`..`A-09`, `BUG-dltl-002`..`007` (không sửa 4 cột gốc). Thêm mục "Cập nhật 2026-09-09 (backend-engineer)" cuối file.
  - `docs/hrm/du_lieu_tinh_luong/api-contract-du-lieu-tinh-luong.md` — thêm Mục 0.5 "Cập nhật 2026-09-09 (Backend Engineer)" ghi rõ các thay đổi hành vi/contract (guard 403 mới, `E-dltl-005` đổi cơ chế trả về từ Zod sang PayrollError, số liệu `standardWorkDays`/BHXH/đoàn phí đổi theo cấu hình).
  - `docs/hrm/architecture/dev-notes.md` — thêm Mục "1.9. Dữ liệu tính lương (`du_lieu_tinh_luong`)" (mô hình nghiệp vụ, bảng thao tác→route/controller/service, nơi ở công thức, 5 điều KHÔNG NHÂN ĐÔI, nợ còn lại).
- KHÔNG đụng: bất kỳ file nào trong `hdđt_maxv/` (đúng ràng buộc — lỗi thứ 9 để dành frontend-engineer). Không đổi `tinhThueLuyTien()` (biểu thuế TNCN) — theo chỉ định tường minh của task, dù ADR-dltl-04 có đề xuất nối `GeneralSetting.taxBrackets` (còn OQ-dltl-002 mở, cần ADR riêng).
- Liên kết: REQ = `srs-du-lieu-tinh-luong.md` BR-dltl-001/002/006/023 · `data-model-du-lieu-tinh-luong.md` ADR-dltl-01..05 · `api-contract-du-lieu-tinh-luong.md` · RVW-001, RVW-002 · BUG-dltl-002, BUG-dltl-003, BUG-dltl-004, BUG-dltl-006, BUG-dltl-007 (kèm `A-01`, `A-02`, `A-03` — Architect) · `CONTEXT_SUMMARY.md` Mục 19.4, 19.9.
- Kiểm chứng (số liệu THẬT, tự chạy):
  - `npm run typecheck` (be_maxv) — **exit 0, 0 lỗi**.
  - `npm run lint` (be_maxv) — **0 error / 340 warning** (không tăng so với trước; 3 warning `no-explicit-any` ở `payrollPeriods.controller.ts:62,70,77` biến mất nhờ bỏ `as any`).
  - `npx tsx --experimental-test-module-mocks --test src/__tests__/hrmPayrollInputData.test.ts` — **11/11 pass** (3 test cũ giữ/sửa + 8 test mới).
  - `npm test` (toàn bộ suite be_maxv) — **642/646 pass**. 4 fail còn lại là `TC-hrm-301`/`TC-hrm-316` (×2, tính cả block cha) trong `hrmSettingsShiftsHolidaysApi.test.ts` — 2 ca đỏ **cố ý không nới lỏng** thuộc cụm "Cấu hình mặc định/Ca làm việc/Lịch ngày lễ" đã tồn tại **TRƯỚC** phiên này (xác nhận qua `CONTEXT_SUMMARY.md` dòng 13-14: "Còn 4 ca đỏ cố ý không nới lỏng"), hoàn toàn không liên quan `du_lieu_tinh_luong` — không phải regression của phiên này.
- Không sửa được / để lại: lỗi thứ 9 (22 file FE mock — ngoài phạm vi, chờ `frontend-engineer` kích hoạt lại). Biểu thuế TNCN chưa nối `GeneralSetting.taxBrackets` — chờ OQ-dltl-002 chốt qua ADR riêng (chi phí cao: cần chốt hình dạng JSON, Zod schema, seed dữ liệu — theo đúng cảnh báo của ADR-dltl-04, không tự ý làm trong phiên bug-fix này). `A-04`..`A-09`, `RVW-003`..`RVW-017` (toàn bộ 🟡/🟢 non-blocking) cố ý chưa đụng — ngoài phạm vi 8 lỗi 🔴 được giao.
- Commit: chưa commit

## [2026-09-09 17:55] frontend-engineer — Nối 23 file FE `hdđt_maxv` còn dùng mock sang API thật (lỗi thứ 9)
- Nhiệm vụ: User kích hoạt tường minh `frontend-engineer` (ngoài pipeline mặc định) để nối nốt 22-23 file component `du_lieu_tinh_luong` còn dùng `mock/hooks/*` (lỗi thứ 9 backend-engineer để lại ở phiên 18:30). Tự `grep -rl "mock/hooks\|mock/"` chốt danh sách THẬT trước khi làm (không tin số liệu ước lượng của báo cáo trước) — ra đúng **23 file**, khớp cận trên ước lượng của Architect/QA.
- Đã đọc để đối chiếu (không sửa): `be_maxv/src/services/client/hrm/du_lieu_tinh_luong/payrollInputs.service.ts` (toàn bộ — nguồn sự thật DUY NHẤT cho response shape thật của 8 GET, vì `payrollInputsApi.ts` khai type SAI, xem phát hiện bên dưới), `catalogs.service.ts` (resolveTargetEmployees, delete-rồi-create trong mọi `apply*`), `payrollPeriods.service.ts`/`payrollPeriodLockGuard.ts`, `nhanVienApi.ts`/`nhanVienQueries.ts`, `holidaysQueries.ts`, `phongBanQueries.ts`, `salaryItemsQueries.ts`, `hrmKeys.ts`, `docs/hrm/du_lieu_tinh_luong/srs-du-lieu-tinh-luong.md` + `api-contract-du-lieu-tinh-luong.md`.
- **Phát hiện quan trọng nhất, KHÔNG có trong bất kỳ báo cáo nào trước** (🔴, chặn build nếu không sửa): `hdđt_maxv/src/features/hrm/api/payrollInputsApi.ts` khai SAI kiểu trả về của cả 8 hàm `get*` (attendance/overtime/kpi/bonus/piecework/commission/diligence/adjustments) — khai `T[]` phẳng (vd `KpiRecordApi[]`) trong khi `payrollInputs.service.ts` thật trả **1 dòng/nhân viên đã gộp sẵn `records[]` + số tổng hợp** (vd `{ma_nv, ho_ten, ma_pb, records, avgScore, totalKpiItems}`). Sai kiểu này không lộ ra cho tới khi component thật cố truy cập `.records`/`.totalAmount`/... trên phần tử mảng — `tsc -b` bắt được ngay (54 lỗi TS2339/TS7006 ở lượt build đầu). Đã sửa: thêm 7 interface `*SummaryApi`/`AttendanceMatrixResponse` đúng response thật, đổi kiểu trả về 8 hàm `get*` trong `payrollInputsApi.ts` — KHÔNG đổi hành vi runtime (chỉ sửa khai báo kiểu), `payrollInputsQueries.ts` tự suy kiểu đúng qua `useQuery` nên không phải sửa.
- Đã tạo:
  - `hdđt_maxv/src/features/hrm/components/du_lieu_tinh_luong/useNhanVienKyLuong.ts` (MỚI — hook composition, KHÔNG phải API client mới) — bản thay `mock/hooks/kyLuong.ts::useNhanVienKyLuong`, giữ nguyên chữ ký + luật lọc (kể cả ô "Loại HĐ" — API `payroll-data/*` không hỗ trợ lọc này phía máy chủ), nguồn dữ liệu đổi sang `api/nhanVienQueries.ts::useNhanVienRows` (đã thật).
- Đã sửa (bổ sung, không đổi hành vi cũ):
  - `api/payrollInputsApi.ts` — sửa 8 kiểu trả về `get*` (xem phát hiện ở trên).
  - `api/payrollCatalogsQueries.ts` — thêm 4 hook map mã→id (`useChiTieuKpiIdByCode`, `useSanPhamIdByCode`, `useLoaiChuyenCanIdByCode`, `useKhoanBuTruIdByCode`), dùng chung `queryKey`/`queryFn` với hook đã có nên KHÔNG tốn thêm request — cần vì UI định danh bằng **mã**, API `apply*`/`record` cần **id** (UUID).
  - `api/salaryItemsQueries.ts` — thêm `useKhoanLuongIdByCode` (cùng lý do, dùng cho Thưởng/Lương phần trăm).
  - `features/hrm/chamCong.ts` — export thêm `oMacDinh()` (trước là hàm nội bộ) để `ChamCongPanel` tái dùng khi mô phỏng "xóa ô" (xem bên dưới), không đổi logic hàm.
  - `components/du_lieu_tinh_luong/ThanhLocKyLuong.tsx` — đổi `usePhongBanList` từ `mock/hooks/phongBan` sang `api/phongBanQueries` (đã thật); **bỏ nút "Lưu thay đổi"** (props `coThayDoi`/`dangLuu`/`onLuu` xóa khỏi interface) — quyết định kiến trúc giải thích ở mục "Quyết định quan trọng" bên dưới. File này chỉ dùng nội bộ trong `du_lieu_tinh_luong/` (đã tự grep xác nhận trước khi đổi contract).
  - 7 module × 3 file (`Panel.tsx`, `DanhSachXCard.tsx`, `TaiSuDungXDialog.tsx`) — **kpi**, **thuong**, **bu_tru**, **chuyen_can**, **luong_san_pham**, **luong_phan_tram**, **tang_ca**: đổi import từ `mock/hooks/*` sang `api/payrollCatalogsQueries.ts` + `api/payrollInputsQueries.ts` (đã thật) + `useNhanVienKyLuong` (mới) + `useCurrentPayrollPeriod` (đã thật, tái dùng context có sẵn từ phiên `fullstack-engineer` 14:48).
  - `components/du_lieu_tinh_luong/cham_cong/ChamCongPanel.tsx` — đổi từ lịch tháng hiện tại (`thangHienTai()`, không gắn kỳ lương) sang đọc `nam`/`thang` từ `useCurrentPayrollPeriod().selectedPeriod` (`month`/`year`); `state.nhanVien`/`state.ngayLe`/`state.chamCong` (mock) → `nhanVienQueries.useNhanVienList()` + `holidaysQueries.useNgayLeList()` (đã thật) + `payrollInputsQueries.useAttendanceMatrix`/`useOverrideAttendanceCell` (đã thật).
- **Quyết định quan trọng (đấu dây có thay đổi hành vi nhỏ, ghi rõ vì không phải "chỉ đổi dòng import"):**
  1. **Bỏ "mẫu đã lưu" độc lập với "áp dụng"** — bản mock có 2 nút ghi riêng ("Lưu thay đổi" lưu bảng nháp không gắn nhân viên nào, "Áp dụng" mới ghi cho nhân viên). API `POST .../apply` thật KHÔNG có endpoint "chỉ lưu, chưa áp" tương ứng — mọi `apply*` đều `deleteMany` rồi `createMany` thẳng cho danh sách nhân viên cụ thể (xác nhận đọc `payrollInputs.service.ts` — cùng 1 pattern cho cả 6 module có "mẫu"). Quyết định: bảng đang soạn (`mau`) là **state cục bộ thuần túy** (`useState`, reset khi đổi kỳ lương), "Lưu thay đổi" bị bỏ, "Áp dụng" là hành động ghi DUY NHẤT. Hợp lý hơn về nghiệp vụ vì kỳ lương giờ là khái niệm thật (mock cũ không có kỳ lương, "mẫu" là 1 bản nháp toàn cục xuyên suốt mọi kỳ).
  2. **"Áp dụng" LUÔN gửi `scope: 'nhan_vien'` + `employeeIds` tường minh**, kể cả khi người dùng chọn phạm vi "Toàn công ty"/"Phòng ban" ở UI — KHÔNG gửi `scope: 'phong_ban'` + `ma_pb` để máy chủ tự `resolveTargetEmployees`, vì máy chủ không biết lọc "Loại HĐ" (chỉ có ở trình duyệt) nên có thể ghi RỘNG HƠN danh sách đang hiển thị — đúng lớp lỗi BUG-HRM-25 (ghi nhầm ngoài phạm vi nhìn thấy) đã từng xảy ra ở phân hệ Hợp đồng.
  3. **"Xóa của 1 nhân viên"** (5/8 module: KPI/Thưởng/Sản phẩm/Phần trăm/Bù trừ) = gọi lại `apply` với `employeeIds: [maNv], items: []` — không có endpoint xóa riêng, nhưng `apply` luôn xóa-trước-khi-ghi nên mảng rỗng tương đương xóa sạch (xác nhận đọc code, không suy đoán).
  4. **Chuyên cần KHÔNG có endpoint áp dụng hàng loạt** (chỉ có `POST .../record` ghi 1 dòng + `DELETE .../:id` xóa 1 dòng) — "Áp dụng chuyên cần cho N người" mô phỏng lại bằng vòng lặp TUẦN TỰ mỗi người (xóa hết bản ghi cũ rồi ghi lại theo bảng đang soạn), báo lỗi kiểu "đã áp X/Y rồi dừng" nếu giữa chừng hỏng (cùng lối `useGanNhanhPhongBan` đã có trong `nhanVienQueries.ts`). Chậm hơn 1 request, chấp nhận được vì đây là thao tác không thường xuyên.
  5. **Tăng ca — cột "Tổng giờ năm" mất chính xác**: API không trả lũy kế các kỳ TRƯỚC trong năm, tạm bằng giờ kỳ hiện tại (ghi chú rõ trong code + JSDoc đầu `TangCaPanel.tsx`). Cần cộng dồn `GET` của mọi kỳ trong năm để đúng — ngoài phạm vi phiên này.
  6. **Chấm công — "Xóa ô" không xóa thật**: API `overrideAttendanceCell` bắt buộc `attendanceType` (không nhận rỗng/null) nên không "xóa" được, chỉ ghi đè lại giá trị MẶC ĐỊNH suy từ lịch (`oMacDinh`) — kết quả hiển thị giống hệt xóa dù CSDL vẫn còn 1 dòng ghi đè. **Bỏ nút "Đặt lại theo lịch chuẩn"** (reset cả bảng) vì không có endpoint xóa/reset hàng loạt.
  7. **KHÔNG tái tạo `kiemTraBang()` phía FE** (kiểm trùng dòng/thiếu chọn/tổng trọng số ≤0 mà bản mock có) — dựa vào backend đã chặn đúng các case này (E-dltl-009/010/011/014/016 xác nhận đọc code) + toast thông điệp lỗi chung qua `getErrorMessage`. Đơn giản hóa có chủ ý do giới hạn thời gian phiên, không phải bỏ sót.
- KHÔNG đụng: bất kỳ file `.ts`/`.prisma` nào trong `be_maxv/` (đúng ràng buộc). Không sửa file gốc trong `mock/hooks/` (giữ nguyên làm tham chiếu).
- Liên kết: REQ = `srs-du-lieu-tinh-luong.md` · `api-contract-du-lieu-tinh-luong.md` Mục 0.5 · BUG lỗi thứ 9 (`CONTEXT_SUMMARY.md` Mục 19.9) · dev-notes.md Mục 1.9.
- Kiểm chứng (số liệu THẬT, tự chạy):
  - `npm run lint` (`hdđt_maxv`) — **exit 0, 0 error/0 warning** (chạy 2 lần, trước và sau khi sửa `payrollInputsApi.ts`, cả hai đều sạch).
  - `npm run build` (`tsc -b && vite build`) — lượt đầu **54 lỗi TS2339/TS7006** (phát hiện type-contract sai ở `payrollInputsApi.ts`, xem trên); sau khi sửa 8 kiểu trả về + 1 điểm truy cập `.records` ở `ChamCongPanel.tsx` (do `getAttendanceMatrix` đổi từ mảng phẳng sang `{employees, records}`) → **build sạch, exit 0**, `12345 modules transformed`, output `dist/` sinh đủ. 2 warning còn lại (`INEFFECTIVE_DYNAMIC_IMPORT` của `exceljs`, chunk >500kB) đã tồn tại từ trước, không liên quan diff phiên này.
  - Verify động (một phần — xem mục "Không xác nhận được" bên dưới): `npm run dev` khởi động sạch (`VITE ready in 376ms`, không log lỗi), `curl` gốc `/` trả 200, `curl` trực tiếp 3 file vừa sửa (`KpiPanel.tsx`, `ChamCongPanel.tsx`, `useNhanVienKyLuong.ts`) qua dev server đều trả 200 (Vite transform qua esbuild/oxc — pipeline KHÁC `tsc -b` — cũng không báo lỗi cú pháp/resolve module).
- **Không xác nhận được (thành thật, KHÔNG tự nhận đã test)**: chưa test tay bằng trình duyệt thật (không có phiên đăng nhập/company/kỳ lương trong môi trường này) — CHƯA xác nhận: (a) luồng network request/response thật khớp đúng kiểu vừa sửa ở `payrollInputsApi.ts` (chỉ xác nhận qua đọc code service, chưa gọi API thật), (b) UI hiển thị đúng khi bấm "Áp dụng"/"Xóa"/"Tái sử dụng" trên dữ liệu thật, (c) hành vi khi kỳ lương khóa sổ (`isReadOnly`) có disable đúng hết các nút như code đã viết hay không, (d) vòng lặp tuần tự "Áp dụng chuyên cần" khi có nhiều nhân viên — chưa đo thời gian thật/chưa xác nhận UX chờ có ổn không.
- Commit: chưa commit

## [2026-09-10 12:02] backend-engineer — code mới
- Nhiệm vụ: Triển khai Phase 3 Backend cho **Bảng lương tổng hợp** — pipeline 10 bước tính thuế/bảo hiểm theo `ADR-010-pipeline-thue-bao-hiem-bang-luong.md`, đúng `BR-dltl-024..027` + quyết định `Q-1`/`Q-2`/`Q-9.3` (chủ dự án chốt 2026-09-10), thêm endpoint `GET /payroll/support-allowances`, mở rộng 3 module chéo (`cau_hinh_mac_dinh`, `cai_dat_luong/salary-items`) theo yêu cầu "ảnh hưởng chéo bắt buộc cùng lượt" của Architect.
- Đã sửa:
  - `be_maxv/prisma/tenant/schema.prisma`:1104-1146 (`GeneralSetting` +3 cột: `lunchAllowanceTaxFreeCap`, `withholdingTaxRate`, `withholdingTaxThreshold`), :1217-1250 (`SalaryItem` +1 cột `isMealAllowance`), :1686-1769 (`PayrollSheetLine` +15 cột theo data-model Mục 11.4 — 11 Decimal + 3 Boolean + 1 String, đếm khớp 29 cũ + 15 mới = 44)
  - `be_maxv/src/constants/hrm/du_lieu_tinh_luong/insuranceCaps.ts` (MỚI, 17 dòng) — 2 hằng số trần bảo hiểm `× 20` có trích dẫn luật (ADR-010 QĐ-2)
  - `be_maxv/src/services/client/hrm/du_lieu_tinh_luong/payrollCalculation.service.ts` (viết lại toàn bộ, 804 dòng) — pipeline 10 bước trong `calculatePayrollPreview` (dòng 269), hàm mới `laHopDongKhauTruTaiNguon()`:100, `laKhoanMienThue()`:132, `tinhKhoanPhuCapTheoKy()`:171 (dùng CHUNG cho 2 endpoint), `getActiveStructureItemMap()` (nội bộ), `getSupportAllowanceBreakdown()`:711 (endpoint mới); `snapshotPayrollSheet()`:663 giữ nguyên cơ chế `createMany` nhưng nay đẩy đủ 44 field khớp schema
  - `be_maxv/src/controllers/client/hrm/du_lieu_tinh_luong/payrollCalculation.controller.ts`:29 (thêm `getSupportAllowances`, cùng guard `dbCoQuyenLuongPayroll`)
  - `be_maxv/src/routes/hrm/du_lieu_tinh_luong/payrollCalculation.route.ts`:7 (thêm route `GET /payroll/support-allowances`)
  - `be_maxv/src/services/client/hrm/cau_hinh_mac_dinh/generalSettings.service.ts`:138-140 (`khoiTaoCauHinhMacDinh()` +3 field mặc định luật định; `updateSettings`/`restoreDefault` không cần sửa vì đã spread `payload` trực tiếp)
  - `be_maxv/src/validators/hrm/cau_hinh_mac_dinh/generalSettings.validator.ts`:159-161 (3 field mới, `E-hrm-083`: `.min(0)`/`withholdingTaxRate` thêm `.max(100)`)
  - `be_maxv/src/validators/hrm/cai_dat_luong/salaryItems.validator.ts`:52,67 (`isMealAllowance` vào `createSalaryItemSchema`/`updateSalaryItemSchema`, mặc định `false`)
  - `be_maxv/src/services/client/hrm/cai_dat_luong/salaryItems.service.ts`:19,35,183,220 (`formatSalaryItem`/`createSalaryItem`/`updateSalaryItem` đọc-ghi `isMealAllowance`)
  - `be_maxv/src/__tests__/hrm/hrmPayrollCalculation.test.ts` (MỚI, 790 dòng, 46 test) — TDD engine thuần (không qua HTTP) cho 9 nhóm test-matrix: Nhóm 1 (hybrid `TC-blth-001..004`), Nhóm 2 (biểu thuế `TC-blth-006..013`), Nhóm 3 (2 trần BH `TC-blth-014..019`), Nhóm 4 (OT miễn thuế + `otBase` QĐ-1 `TC-blth-020..024c`), Nhóm 5 (khấu trừ 10% `TC-blth-025..029`), Nhóm 6 (trần ăn trưa `TC-blth-030..034` + `AC-dltl-23`), Nhóm 7 (support-allowances + bất biến `TC-blth-035..039`), Nhóm 8 (regression `TC-blth-041/044`), Nhóm 9 (Q-1/Q-2/Q-9.3 `TC-blth-045..050`)
  - `be_maxv/src/__tests__/hrm/hrmPayrollInputData.test.ts` — `DEFAULT_GENERAL_SETTING` thêm `baseSalary`/`regionMinSalary`/3 field mới (bắt buộc, nếu không pipeline mới ra `NaN`); mock `salaryItem.findMany`/`salaryStructure.findMany` (mới, hỗ trợ `options.salaryItems`/`options.salaryStructures`); thêm 1 test HTTP end-to-end cho `/payroll/support-allowances` (200 + bất biến khớp `/payroll/calculate` + 403 + 404) — 11 test cũ giữ nguyên PASS không sửa số liệu
  - `be_maxv/src/__tests__/hrm/hrmSalarySettings.test.ts` — 2 lời gọi `createSalaryItem()` thêm `isMealAllowance: false` (bắt buộc do kiểu `CreateSalaryItemInput` sau khi thêm field có default)
  - `be_maxv/src/__tests__/hrm/hrmSalarySettingsApi.test.ts` — thêm 2 test HTTP (`POST`/`PATCH /salary-items` với `isMealAllowance`)
  - `be_maxv/src/__tests__/hrm/hrmSettingsShiftsHolidays.test.ts` — `khoiTaoCauHinhMacDinh()` test thêm assertion 3 field mới
  - `be_maxv/src/__tests__/hrm/hrmSettingsShiftsHolidaysApi.test.ts` — TC-hrm-273 (GET tự khởi tạo) + TC-hrm-283 (restore-default) thêm assertion 3 field mới; thêm test mới `ADR-010 QĐ-2` (PUT 3 field + E-hrm-083 boundary 400) — chạy trên DB Postgres thật (provision/drop tenant), không chỉ mock
  - `docs/hrm/architecture/dev-notes.md` — frontmatter `updated: 2026-09-10` + 3 link mới; thêm Mục 1.10 "Bảng lương tổng hợp — pipeline 10 bước" (mô hình nghiệp vụ, bảng thao tác→route/service, nơi ở công thức, 6 điều TUYỆT ĐỐI KHÔNG NHÂN ĐÔI, mục "Còn nợ" gồm cảnh báo mâu thuẫn số liệu Nhóm 6 — xem bên dưới)
- Liên kết: `ADR-010-pipeline-thue-bao-hiem-bang-luong.md` · `srs-du-lieu-tinh-luong.md` Mục 15 (`BR-dltl-024..027`, `AC-dltl-12..28`) · `data-model-du-lieu-tinh-luong.md` Mục 11 · `api-contract-du-lieu-tinh-luong.md` Mục 8 · `test-matrix-bang-luong-tong-hop.md` (`TC-blth-001..050`)
- Kiểm chứng (số liệu THẬT, tự chạy):
  - `npm run typecheck` — **exit 0**, không lỗi (một lượt gặp `error TS1131/TS1109/TS1003/TS1161/TS1128` do docblock Prisma tự sinh `*/` giữa chừng comment JSDoc — đã sửa câu chữ trong schema, `npm run generate` lại, hết lỗi).
  - `npm run lint` — **0 error**, 359 warning (tăng từ baseline 340 do 17 warning `no-explicit-any` mới trong `hrmPayrollCalculation.test.ts`, cùng convention `any` các file mock DB khác trong repo đã dùng; KHÔNG có warning nào trong code sản xuất mới).
  - `npx tsx --test src/__tests__/hrm/hrmPayrollCalculation.test.ts` — **46/46 PASS**.
  - `npm test` (toàn bộ `be_maxv`, glob `src/__tests__/**/*.test.ts`) — **696 test, 692 PASS, 4 FAIL**. 4 fail đều là 2 ca đỏ CỐ Ý đã ghi nhận từ trước phiên này (`TC-hrm-301` tìm kiếm không bỏ dấu, `TC-hrm-316` thiếu trường "thứ trong tuần" — mỗi ca báo 2 lần do Node test runner đếm cả suite cha), xác nhận bằng cách so khớp đúng tên + nội dung lỗi với `docs/hrm/CONTEXT_SUMMARY.md` Mục 1 ("Còn 4 ca đỏ cố ý không nới lỏng"). **0 lỗi sản phẩm mới do phiên này gây ra.** Tổng test tăng 646→696 (+50: 46 mới trong `hrmPayrollCalculation.test.ts` + 1 HTTP mới trong `hrmPayrollInputData.test.ts` + 2 HTTP mới `isMealAllowance` + 1 HTTP mới `ADR-010 QĐ-2`).
  - Riêng phân hệ payroll (`hrmPayrollCalculation.test.ts` + `hrmPayrollInputData.test.ts`): **58/58 PASS** (46+12).
  - `hrmSettingsShiftsHolidaysApi.test.ts` chạy trên **Postgres thật** (tự provision 2 tenant DB `maxv_9970000001_app`/`maxv_9970000002_app` qua `prisma db push` với schema mới rồi DROP lúc dọn) — xác nhận 19 cột mới lên DB thật KHÔNG lỗi migration, `78 test, 74 PASS, 4 FAIL` (cùng 2 ca đỏ cố ý ở trên).
- **Vướng cần Tester-QA/Code-Reviewer chú ý (không chặn, đã ghi trong dev-notes.md Mục 1.10 "Còn nợ"):** `test-matrix-bang-luong-tong-hop.md` Nhóm 6 (`TC-blth-032`/`033`) tính `hanMucMienThue` KHÔNG quy đổi theo công (giữ 730.000 cố định), mâu thuẫn trực tiếp với chính `AC-dltl-23` (SRS 15.3.1, đã qua BA Final Sign-off) và `ADR-010` bước [5c] — cả hai đều yêu cầu quy đổi trần theo `tyLeCong`. Đã hiện thực đúng theo SRS/ADR (nguồn thẩm quyền cao hơn theo đúng chỉ đạo của nhiệm vụ), và viết lại 2 test case này với số liệu đã sửa (615.385/561.538/53.847 và 923.077/561.538/361.539 thay vì số của bảng test-matrix cũ) kèm chú thích rõ ràng trong `hrmPayrollCalculation.test.ts`. Đề nghị BA/QA xác nhận đây là lỗi đánh máy của QA (quên quy đổi công cho vế trần), không phải thay đổi nghiệp vụ.
  - Chưa chạy `npm run sync:tenants` để đẩy 19 cột mới lên 10 tenant DB thật của khách hàng (theo `data-model` Mục 11.7) — đây là bước vận hành cần Architect/DevOps xác nhận thời điểm, KHÔNG tự ý chạy trong phiên code. `hrmSettingsShiftsHolidaysApi.test.ts` đã CHỨNG MINH migration `db push` hoạt động đúng trên DB thật (qua 2 tenant test tự provision/drop), nên rủi ro kỹ thuật của bước này thấp.
  - Chưa viết assertion field-level đầy đủ cho toàn bộ 44 trường response của `GET /payroll/calculate` qua HTTP (chỉ test qua `calculatePayrollPreview()` trực tiếp — nhanh và chính xác hơn, nhưng không phủ layer serialize JSON/Decimal→string của `sendOk`). `A-06` (Decimal ra chuỗi ở nhánh snapshot) vẫn là nợ kỹ thuật đã ghi nhận từ trước, chưa đóng trong phiên này (đúng phạm vi ADR-010 — việc tách riêng).
- Commit: chưa commit

## [2026-09-10 15:40] backend-engineer — fix RVW-018, RVW-019, RVW-020, RVW-021, RVW-022, RVW-023, RVW-025
- Nhiệm vụ: Vòng sửa lỗi sau Code Review "Review 2026-09-10" (`docs/hrm/review-findings.md`) cho chức năng Bảng lương tổng hợp — đóng 1 finding 🔴 blocking (RVW-018: RBAC + audit log thiếu ở `/salary-items`) và 6 finding 🟡/🟢 được yêu cầu sửa cùng lượt.
- Đã sửa:
  - `be_maxv/src/routes/hrm/cai_dat_luong/salaryItems.route.ts`:1-31 — import `assertAdminOrOwner`, gắn `{ preHandler: assertAdminOrOwner }` cho `POST`/`PATCH`/`DELETE /salary-items` (RVW-018). Quyết định: siết cả 3 route (không tách quyền hẹp theo field), lý do ghi trong comment.
  - `be_maxv/src/controllers/client/hrm/cai_dat_luong/salaryItems.controller.ts`:5,20-38,45-70 — thêm helper `ghiNhatKyKhoanLuong()` (writeLog), gọi sau `create`/`update`/`remove` (RVW-018).
  - `be_maxv/src/services/client/hrm/cai_dat_luong/salaryItems.service.ts`:250-256 — `deleteSalaryItem` trả `{ code }` để controller ghi audit log đúng khóa nghiệp vụ (RVW-018).
  - `be_maxv/src/services/client/hrm/du_lieu_tinh_luong/payrollCalculation.service.ts`:11 (import `SINGLETON_ID`, RVW-025), :305,757 (`findFirst()` → `findUnique({where:{id:SINGLETON_ID}})`, RVW-025), :317-327 và :763-773 (lọc `employeeSalary` theo `effectiveFrom`/`effectiveTo` phủ kỳ, RVW-021), :711-729 (docstring cập nhật RVW-019/020), :808-818 (`total`/`monthlyTotal` chỉ cộng khoản còn trong `columnCodes` — cùng quy tắc lọc `status='ACTIVE'` với `columns`, RVW-019), :820-829 (trả kèm `periodStatus` + `isLiveRecalculated: true`, RVW-020).
  - `docs/hrm/architecture/api-contract.md` — thêm mới `## 7D. Cấu hình mặc định — 3 tham số thuế mới` (trước Mục 8) — tài liệu KHÔNG thực sự có "Mục 7D.0" như review dẫn (đã grep xác nhận, đây là drift tài liệu có từ trước) (RVW-023).
  - `docs/hrm/cai_dat_luong/api-contract-cai-dat-luong.md` Mục 1 — thêm `isMealAllowance` vào response/request mẫu, ghi quyền `assertAdminOrOwner` cho 3 route ghi, sửa `PUT`→`PATCH` cho khớp code (RVW-023).
  - `docs/hrm/review-findings.md` — cập nhật `OPEN → FIXED` cho RVW-018/019/020/021/022/023/025 kèm bằng chứng, thêm mục "Cập nhật 2026-09-10" cuối file.
  - Test mới/sửa:
    - `be_maxv/src/__tests__/hrm/hrmSalarySettingsApi.test.ts` — import `errorHandlerPlugin`, đăng ký trong `buildTestApp` (tham số hóa `role`), thêm spy `writeLog`, thêm test "RVW-018: POST/PATCH/DELETE /salary-items chặn role không phải ADMIN/OWNER, và ghi audit log khi thành công".
    - `be_maxv/src/__tests__/hrm/hrmPayrollCalculation.test.ts` — mock `db.generalSetting` thêm `findUnique`, mock `employeeSalary.findMany` mô phỏng lọc `effectiveFrom`/`effectiveTo`; thêm 8 test mới nhóm "Nhóm 9": RVW-019 (×1), RVW-020 (×1), RVW-021 (×4), RVW-025 (×1).
    - `be_maxv/src/__tests__/hrm/hrmPayrollInputData.test.ts` — mock `db.generalSetting` (2 chỗ dùng chung factory) thêm `findUnique` để không vỡ theo RVW-025.
- Liên kết: RVW-018 · RVW-019 · RVW-020 · RVW-021 · RVW-022 · RVW-023 · RVW-025 · `review-findings.md` Review 2026-09-10 · `ADR-010-pipeline-thue-bao-hiem-bang-luong.md`.
- Vận hành: chạy `npm run sync:tenants` trên môi trường dev/local (`.env.local`, `localhost:5432`) — **10/10 tenant thành công, 0 lỗi** (RVW-022). Production 10 tenant khách hàng CHƯA chạy (cố ý, cần Architect/DevOps xác nhận thời điểm).
- Kiểm chứng (số liệu THẬT, tự chạy):
  - `npm run typecheck` — **exit 0**, 0 lỗi.
  - `npm run lint` — **0 error / 365 warning** (không tăng so với baseline trước đợt; không có warning mới ở bất kỳ file vừa sửa).
  - `npx tsx --experimental-test-module-mocks --test src/__tests__/hrm/hrmPayrollCalculation.test.ts` — **53/53 PASS** (45 cũ + 8 mới).
  - `npx tsx --experimental-test-module-mocks --test src/__tests__/hrm/hrmSalarySettingsApi.test.ts` — **13/13 PASS** (12 cũ + 1 mới, sau khi sửa lỗi thứ tự reset spy `ghiNhatKyKhoanLuongCalls`).
  - `npm test` (toàn bộ suite) — **704 test, 700 PASS, 4 FAIL**. 4 fail = `TC-hrm-301`/`TC-hrm-316` (×2, đếm cả parent block) — 2 ca đỏ CỐ Ý không nới lỏng, có từ TRƯỚC phiên này, không liên quan module vừa sửa. **0 lỗi sản phẩm mới.**
- **Chưa sửa (cố ý, ngoài phạm vi "không bắt buộc" của lượt này):** `RVW-024` (đổi số liệu ghim test tài chính quan trọng — rủi ro hơn mức "tiện tay"), `RVW-026` (đổi tên field hợp đồng API cần FE đồng thuận trước). Cả hai vẫn `OPEN`, không đụng tới.
- Commit: chưa commit

## [2026-09-10 17:30] frontend-engineer — code mới
- Nhiệm vụ: Kích hoạt ngoại lệ theo yêu cầu tường minh của user (frontend-engineer bình thường tạm ngừng) — thay mock trong `hdđt_maxv/src/features/hrm/components/bang_luong` bằng API thật `GET /payroll/calculate` + `GET /payroll/support-allowances` (api-contract-du-lieu-tinh-luong.md Mục 8, engine BE đã qua đủ vòng BA → Architect → QA → Backend → Code Review).
- Đã sửa/tạo:
  - `hdđt_maxv/src/features/hrm/api/du_lieu_tinh_luong/payrollCalculationApi.ts` (viết lại toàn bộ) — kiểu `PayrollCalculationLineApi` (44 trường), `SupportAllowanceColumnApi`/`SupportAllowanceItemApi`/`SupportAllowanceResponseApi`, 3 hàm `getPayrollCalculate`/`getSupportAllowances`/`getPayrollSheetLines`. Bản cũ đoán sai shape response (chưa từng ai import) — xóa hẳn, viết lại khớp đúng `payrollCalculation.service.ts` thật.
  - `hdđt_maxv/src/features/hrm/api/du_lieu_tinh_luong/payrollCalculationQueries.ts` (viết lại toàn bộ) — `usePayrollCalculateQuery`/`useSupportAllowancesQuery`/`usePayrollSheetLinesQuery` (raw `useQuery`, chưa đấu dây `sheetLines` vào UI nào).
  - `hdđt_maxv/src/features/hrm/api/hrmKeys.ts`:169-183 — thêm `hrmPayrollCalculationKeys.supportAllowances(companyId, periodId)`, cùng tiền tố `all` để `useInvalidatePayroll()` tự quét.
  - `hdđt_maxv/src/features/hrm/api/bang_luong/bangLuongQueries.ts` (MỚI, folder mới) — adapter duy nhất: `useBangLuongRows`, `useLuongHoTroRows`, `useKyBangLuong`, `useSoNhanVienDangLam` — đổi 44 trường API sang `DongBangLuong`/`DongLuongHoTro`, xử lý 2 cạm bẫy field (`otRawHours` vs `otConvertedHours`; `thu_nhap_chiu_thue` suy 4 số hạng thay vì dùng thẳng `taxableIncome`), lọc chéo phòng ban qua `useNhanVienRows()` vì payroll API chỉ trả tên phòng ban không trả mã.
  - `hdđt_maxv/src/features/hrm/types/index.ts` — thêm `KhoanHoTroCot` (cột động tab "Lương hỗ trợ", KHÔNG dùng lại `KhoanLuong` vì API chỉ trả 4 trường mô tả cột).
  - `hdđt_maxv/src/features/hrm/calculations/bang_luong/bangLuong.ts` (viết lại) — XÓA `tinhDongBangLuong`/`thueLuyTien`/`lyDoKhongTinhDuocLuong`/`NguonTinhLuong`/`LOI_BIEU_THUE_KHONG_DU_BAC` (nghiệp vụ tính thuế/bảo hiểm, nay là nguồn sự thật DUY NHẤT ở `be_maxv`/ADR-010). GIỮ `tienTheoCheDo`/`hauToCheDo`/`CHE_DO_HIEN_THI`/`tongBangLuong` (thuần format hiển thị + cộng dồn, không tính lại số).
  - `hdđt_maxv/src/features/hrm/mock/hooks/bangLuong.ts` — **XÓA HẲN** (khác các mock khác trong feature, giữ lại sẽ vỡ `tsc` vì import thẳng các hàm vừa xóa ở trên; không còn ai import sau khi sửa 2 component tiêu thụ).
  - `hdđt_maxv/src/features/hrm/components/bang_luong/BangLuongPanel.tsx` (viết lại) — dùng `useBangLuongRows`/`useKyBangLuong`/`useSoNhanVienDangLam` mới; nút "Tính lại lương" đổi từ tăng `nonce` sang gọi `refetch()`; thêm `Alert` info khi chưa chọn kỳ lương + `Alert` error khi query lỗi.
  - `hdđt_maxv/src/features/hrm/components/bang_luong/BangLuongTable.tsx`:1-25,51,104-223 — thêm prop `isLoading`, vẽ `CircularProgress` trong chính `TableBody` (giữ khung bảng/sticky header khi đang tải), tách rõ 3 nhánh loading/success/empty.
  - `hdđt_maxv/src/features/hrm/components/bang_luong/LuongHoTroPanel.tsx` (viết lại) — dùng `useLuongHoTroRows`/`useKyBangLuong`/`useSoNhanVienDangLam` mới, `columns` lấy từ cùng hook thay vì gọi `useKhoanHoTroList()` riêng; cùng mẫu loading/error/empty với `BangLuongTable`.
  - `hdđt_maxv/src/features/hrm/components/bang_luong/luongHoTroExcel.ts`:9,48 — đổi kiểu tham số `khoanHoTro` từ `KhoanLuong[]` sang `KhoanHoTroCot[]`.
  - `hdđt_maxv/src/features/hrm/components/bang_luong/ThanhLocBangLuong.tsx`:12 — sửa luôn bug tiện thể: import `usePhongBanList` từ `mock/hooks/phongBan` (kho giả) → `api/du_lieu_nhan_vien/phongBanQueries` (API thật, cùng hook `ThanhLocKyLuong.tsx` đang dùng). Không phải lỗi do phiên này tạo ra nhưng nằm trong phạm vi file phải chạm.
  - `hdđt_maxv/src/pages/hrm/bang_luong/BangLuongPage.tsx` (viết lại) — bọc `PayrollPeriodProvider` + render `KyLuongSelector` (TÁI SỬ DỤNG nguyên component của khu "Dữ liệu tính lương", không tự chế cơ chế chọn kỳ mới). Trước đó khu "Bảng lương" không có khái niệm kỳ (mock luôn lấy "tháng hiện tại").
  - `hdđt_maxv/src/features/hrm/api/cau_hinh_mac_dinh/cauHinhQueries.ts`:16-21 — sửa docstring: bỏ nhắc `mock/hooks/bangLuong.ts` khỏi danh sách "nguồn cấu hình dùng chung" (bảng lương KHÔNG còn dùng `useCauHinh()` — BE tự đọc `GeneralSetting`).
  - `docs/hrm/architecture/dev-notes.md` — thêm Mục 2.12 (section Frontend, không đụng phần Backend) — luồng dữ liệu, 2 cạm bẫy field, cơ chế dùng chung kỳ lương, query key mới, 4 trạng thái Loading/Error/Empty/Success, bảng "Chưa làm" (gồm việc CHƯA đấu `/payroll/sheet-lines` cho kỳ đã khóa sổ).
- Liên kết: `api-contract-du-lieu-tinh-luong.md` Mục 8 · `srs-du-lieu-tinh-luong.md` Mục 15 · `payrollCalculation.service.ts` (nguồn thật).
- Kiểm chứng (số liệu THẬT, tự chạy trong `hdđt_maxv/`):
  - `npx tsc -b` — **exit 0**, 0 lỗi.
  - `npm run lint` — **0 lỗi, 0 cảnh báo**.
  - `npm run build` (`tsc -b && vite build`) — **build thành công** (12346 module, `dist/` sinh đủ file; 2 cảnh báo Rolldown về dynamic-import `exceljs` và chunk >500kB đã tồn tại từ trước, không phải do phiên này).
  - Dev server: xác nhận `hdđt_maxv` (cổng 5173) và `be_maxv` (cổng 4000) ĐANG CHẠY sẵn trong môi trường. Dùng skill `chrome-devtools` mở `http://localhost:5173/hrm/bang-luong/bang-luong` LÚC CHƯA ĐĂNG NHẬP: không có lỗi JS trong console (chỉ 401 kỳ vọng từ `/auth/me`), hành vi "không tự chuyển hướng /login khi vào thẳng route con" GIỐNG HỆT route cũ chưa từng đụng `/hrm/du-lieu-luong/cham-cong` (đối chứng song song) — xác nhận đây là hành vi ĐÃ CÓ SẴN của `ProtectedRoute`, không phải lỗi do thay đổi lần này gây ra.
  - **Không xác nhận được (thành thật, KHÔNG tự nhận đã test UI thật)**: KHÔNG có tài khoản/tenant test trong môi trường (đã tìm trong `docs/`, script seed của `be_maxv` — không thấy credential nào) nên CHƯA xem được bảng lương với dữ liệu thật qua trình duyệt: chưa xác nhận (a) 18 cột hiển thị đúng số so với response thật, (b) tab "Lương hỗ trợ" cộng khớp với cột "Thu nhập" của tab Bảng lương (bất biến 🔴 api-contract 8.2), (c) chọn/tạo kỳ lương ở `KyLuongSelector` hoạt động đúng khi mở từ `/hrm/bang-luong`, (d) Xuất Excel ra file đúng định dạng với dữ liệu thật, (e) UX khi kỳ chưa có nhân viên/công ty chưa tạo kỳ lương nào.
- Commit: chưa commit

## [2026-09-10 18:15] frontend-engineer — fix nợ #1 Mục 2.12: `BangLuongPanel` đổi nguồn sang `/payroll/sheet-lines`
- Nhiệm vụ: Tiếp nối trực tiếp phiên 17:30 cùng ngày — sửa đúng khoảng nợ tự phát hiện: `BangLuongPanel` đang gọi `GET /payroll/calculate` (LUÔN tính live) cho MỌI kỳ lương kể cả kỳ đã khóa sổ, vi phạm nguyên tắc "khóa sổ = snapshot bất biến" (`CONTEXT_SUMMARY.md` Mục 1).
- Đã xác nhận TRƯỚC khi sửa (đúng yêu cầu — không tự thêm route nếu thiếu): route `GET /payroll/sheet-lines` **đã tồn tại sẵn** (`be_maxv/src/routes/hrm/du_lieu_tinh_luong/payrollCalculation.route.ts`:6, controller `getSheetLines` tại `payrollCalculation.controller.ts`:21-25) và service `getPayrollSheetLines()` (`payrollCalculation.service.ts`:700-713) **đã tự chuyển nhánh đúng**: `DRAFT`/`PENDING_REVIEW` → gọi lại `calculatePayrollPreview()` (live); `LOCKED`/`APPROVED`/`PAID`/`ARCHIVED` → đọc thẳng `db.payrollSheetLine.findMany({ where: { periodId } })` (snapshot đóng băng). **Không có gap backend** — không cần kích hoạt backend-engineer, không tự thêm route.
- Đã sửa/tạo (`hdđt_maxv/`):
  - `src/features/hrm/api/bang_luong/bangLuongQueries.ts`:31-35,175-185,268-272 — `useBangLuongRows` và `useSoNhanVienDangLam` đổi từ `usePayrollCalculateQuery` sang `usePayrollSheetLinesQuery` (hook đã viết sẵn từ phiên 17:30, chưa từng đấu dây); cập nhật docstring đầu file + 2 hàm liên quan giải thích nguồn dữ liệu mới.
  - `src/features/hrm/api/du_lieu_tinh_luong/payrollCalculationApi.ts` — **phát hiện + tự sửa ở biên FE (KHÔNG đụng backend)** bug ẩn A-06 (nợ kỹ thuật đã ghi nhận từ review-findings.md/api-contract Mục 0.3, BE cố ý chưa đóng): nhánh snapshot trả thẳng cột `Decimal` Prisma — serialize ra CHUỖI qua `Decimal.prototype.toJSON()`, khác nhánh live (`calculatePayrollPreview` đã `Number(...)` mọi trường). Cùng kiểu khai báo `PayrollCalculationLineApi` nhưng runtime đổi kiểu giữa 2 nhánh — không ném lỗi, chỉ âm thầm sai số khi cộng chuỗi (`"1000000"+"500000"` = `"1000000500000"` thay vì `1500000`), đúng chỗ `veDongBangLuong()` cộng `proratedWorkSalary + allowanceInPeriodTotal`. Thêm `PAYROLL_LINE_NUMERIC_FIELDS` (34 trường Decimal/Int liệt kê tường minh) + `normalizePayrollLine()` (`Number(...)` từng trường), áp trong `getPayrollSheetLines()` trước khi trả về — từ điểm này `bangLuongQueries.ts` tin `number` thật cho cả 2 nhánh, không cần tự phòng thủ lại.
  - `src/features/hrm/api/du_lieu_tinh_luong/payrollCalculationQueries.ts` — cập nhật docstring `usePayrollSheetLinesQuery` (bỏ ghi chú "chưa có màn nào gọi").
  - `src/features/hrm/components/bang_luong/BangLuongPanel.tsx` — thêm `useCurrentPayrollPeriod().isLocked`; hiện `Chip` "Đã khóa sổ — số liệu đã chốt" cạnh tên kỳ khi khóa; đổi nhãn nút "Tính lại lương"→"Tải lại số liệu" (icon `CalculateRounded`→`RefreshRounded`) + toast tương ứng khi kỳ đã khóa — tránh hiểu lầm bấm nút sẽ tính lại theo dữ liệu hiện tại.
  - `src/features/hrm/components/bang_luong/LuongHoTroPanel.tsx` — thêm `Alert severity="warning"` khi `isLocked`, cảnh báo tab "Lương hỗ trợ" CHƯA có snapshot riêng (RVW-020 be_maxv), vẫn luôn tính live — giảm nhẹ khoản nợ #3 Mục 2.12 (chưa dùng đúng field `isLiveRecalculated`/`periodStatus` của response, dùng cờ context tương đương thay thế, ghi rõ trong dev-notes).
  - `docs/hrm/architecture/dev-notes.md` — thêm Mục 2.13 (chi tiết đầy đủ), cập nhật bảng "Chưa làm" của Mục 2.12 (đóng dòng #1, giảm nhẹ dòng #3).
- KHÔNG đụng: `useLuongHoTroRows`/`/payroll/support-allowances` (vẫn luôn tính live — backend chưa có snapshot bóc tách theo khoản, nợ thiết kế riêng ngoài phạm vi); không xóa `usePayrollCalculateQuery` (không còn nơi gọi trực tiếp sau đổi này nhưng giữ lại cho nhu cầu xem số tính lại tức thời sau này); không đụng bất kỳ file `be_maxv/`.
- Liên kết: `docs/hrm/architecture/dev-notes.md` Mục 2.12, 2.13 · `api-contract-du-lieu-tinh-luong.md` Mục 8.3, 0.3 (A-06) · `review-findings.md` A-06, RVW-020 · `CONTEXT_SUMMARY.md` Mục 1 "Snapshot bất biến".
- Kiểm chứng (số liệu THẬT, tự chạy trong `hdđt_maxv/`):
  - `npx tsc -b` — **exit 0**, 0 lỗi.
  - `npm run lint` — **0 lỗi, 0 cảnh báo**.
  - `npm run build` (`tsc -b && vite build`) — **build thành công**, 12346 module transformed, `dist/` sinh đủ file; 2 cảnh báo Rolldown (dynamic-import `exceljs`, chunk >500kB) đã tồn tại từ trước, không phải do phiên này.
- **Không xác nhận được (thành thật, KHÔNG tự nhận đã test UI thật)**: không có tài khoản/tenant test trong môi trường (cùng lý do phiên 17:30) nên CHƯA test tay qua trình duyệt: (a) số hiển thị ở kỳ LOCKED khớp đúng snapshot đã lưu lúc khóa sổ, (b) badge/nút đổi nhãn đúng khi chuyển qua lại DRAFT ↔ LOCKED, (c) `normalizePayrollLine()` áp đúng cho response thật (chỉ verify qua đọc code service + kiểu cột Prisma schema, chưa gọi API thật).
- Commit: chưa commit

